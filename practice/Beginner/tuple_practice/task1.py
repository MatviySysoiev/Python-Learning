# 1. Create a single-element tuple named `single_item` containing the integer 42.
#    (Hint: Pay attention to the syntax required for a 1-element tuple!)
# 2. Given the following tuple:
point = (10, 20, 30, 40, 50)
#    Extract and print the middle element (30) using positive indexing.
# 3. Extract and print the last element (50) using negative indexing.

single_item = tuple([42])

print(type(single_item))
